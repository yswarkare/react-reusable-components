import React from 'react';

const Home = () => {
  return (
    <div id={`home-page`} className={`flex flex-col justify-center content-center items-center`}>
      <span className='text-12 text-gray-700'>Home</span>
    </div>
  );
}

export default Home;
